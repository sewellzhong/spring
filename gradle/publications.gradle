apply plugin: "maven-publish"
// Disabled for local source study: Artifactory publishing is not needed for IDE import
// or normal module compilation, and the legacy plugin dependencies may require restricted access.
// apply plugin: 'com.jfrog.artifactory'

publishing {
	publications {
		mavenJava(MavenPublication) {
			pom {
				afterEvaluate {
					name = project.description
					description = project.description
				}
				url = "https://github.com/spring-projects/spring-framework"
				organization {
					name = "Spring IO"
					url = "https://spring.io/projects/spring-framework"
				}
				licenses {
					license {
						name = "Apache License, Version 2.0"
						url = "https://www.apache.org/licenses/LICENSE-2.0"
						distribution = "repo"
					}
				}
				scm {
					url = "https://github.com/spring-projects/spring-framework"
					connection = "scm:git:git://github.com/spring-projects/spring-framework"
					developerConnection = "scm:git:git://github.com/spring-projects/spring-framework"
				}
				developers {
					developer {
						id = "jhoeller"
						name = "Juergen Hoeller"
						email = "jhoeller@pivotal.io"
					}
				}
				issueManagement {
					system = "GitHub"
					url = "https://github.com/spring-projects/spring-framework/issues"
				}
			}
			versionMapping {
				usage('java-api') {
					fromResolutionResult()
				}
				usage('java-runtime') {
					fromResolutionResult()
				}
			}
		}
	}
}

// Disabled together with the Artifactory plugin above. Keep the Maven publication metadata,
// but do not register the Artifactory publish task in local learning environments.
// artifactoryPublish {
// 	publications(publishing.publications.mavenJava)
// }
